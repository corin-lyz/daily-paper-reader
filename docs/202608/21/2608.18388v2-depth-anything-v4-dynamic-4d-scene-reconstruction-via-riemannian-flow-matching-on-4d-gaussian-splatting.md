---
title: "Depth Anything V4: Dynamic 4D Scene Reconstruction via Riemannian Flow Matching on 4D Gaussian Splatting"
title_zh: Depth Anything V4：基于4D高斯溅射的黎曼流匹配用于动态4D场景重建
authors: "Jiaming Fan, Jian Lu, Jinling Jia, Chenbin Zhang"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.18388v2"
tags: ["query:mono-depth"]
score: 9.0
evidence: Depth Anything V4框架用于单目动态场景重建
tldr: 动态4D场景重建面临依赖人工深度标签和计算成本高昂的难题，现有4D高斯泼溅方法直接在欧氏空间优化，容易产生无效的中间状态。本文提出DAV4，将Riemannian Flow Matching应用于4D高斯泼溅的尺度、旋转、不透明度等流形参数，在非欧几里得空间上定义概率路径，保证所有中间状态合法有效。通过精心设计的控制实验，RFM相对于确定性MLP基线在F-score上获得0.044的独立提升（0.762→0.806），该增益来自RFM本身而非测试时优化或预训练；预训练需360 GPU小时，但可在超过一万个场景的大规模部署中摊销。最终，DAV4无需人工标注深度，就能在动态重建和新视图合成上超越先前Depth Anything与逐场景4D-GS，同时通过负高斯对数似然和期望校准误差提供不确定性估计。
source: arxiv
selection_source: fresh_fetch
motivation: 现有动态4D重建依赖人工深度标签且直接在欧氏空间优化易产生无效参数，需要无需标注且数学有效的生成框架。
method: 将Riemannian Flow Matching应用到4D高斯泼溅的流形参数（尺度、旋转、不透明度），定义合法概率路径，并结合测试时优化。
result: 控制实验显示RFM相对MLP基线F-score提升0.044，预训练360 GPU小时可摊销，DAV4在无标注下优于先前模型。
conclusion: 实现无需人工深度标签的鲁棒动态重建，为大规模4D场景部署提供高效可扩展方案。
---

## 摘要
我们提出了Depth Anything V4 (DAV4)，一个从单目视频进行动态4D场景重建的框架。我们的关键贡献是将黎曼流匹配（RFM）应用于4D高斯溅射参数，直接在非欧几里得流形（尺度、旋转、不透明度）上定义概率路径，确保所有中间状态都是有效的。通过受控实验，我们将RFM的贡献与测试时优化（TTO）和预训练分离开来。一个使用相同数据、架构和TTO的确定性MLP基线达到F-score 0.762；RFM达到0.806——+0.044的增益是RFM的独立贡献。我们提供了修正的计算成本分析：预训练为360 GPU小时，对于大规模部署（超过10,000个场景）是摊销的。不确定性通过负高斯对数似然和期望校准误差进行量化。DAV4在动态重建和新视角合成方面优于之前的Depth Anything模型和逐场景4D-GS，同时不使用人类标注的深度标签作为训练损失。

## Abstract
We present Depth Anything V4 (DAV4), a framework for dynamic 4D scene reconstruction from monocular video. Our key contribution is the application of Riemannian Flow Matching (RFM) to 4D Gaussian Splatting parameters, defining probability paths directly on non-Euclidean manifolds (scale, rotation, opacity), ensuring all intermediate states are valid. Through controlled experiments, we isolate RFM's contribution from test-time optimization (TTO) and pre-training. A deterministic MLP baseline with the same data, architecture, and TTO achieves F-score 0.762; RFM achieves 0.806 - the +0.044 gain is RFM's isolated contribution. We provide corrected computational cost analysis: pre-training is 360 GPU-hours, amortizing for large-scale deployment (over 10,000 scenes). Uncertainty is quantified via Negative Gaussian Log-Likelihood and Expected Calibration Error. DAV4 outperforms prior Depth Anything models and per-scene 4D-GS on dynamic reconstruction and novel-view synthesis, while using no human-annotated depth labels as training losses.

---

## 论文详细总结（自动生成）

根据提供的材料，arXiv 页面显示该版本（2608.18388v2）已被撤回，完整 PDF 不可用；以下总结基于摘要与元数据信息。

## 1. 核心问题与研究动机
- 动态 4D 场景重建（从单目视频中恢复动态 3D 场景）具有重要意义，但现有方法存在两大痛点：
  - 依赖人类标注的深度标签作为训练损失，缺乏标注数据时难以推广。
  - 已有 4D Gaussian Splatting（4D-GS）方法直接在欧氏空间优化高斯参数，容易产生无效的中间状态（如负尺度或非法旋转），影响重建质量与稳定性。
- 本文提出 Depth Anything V4（DAV4），目标是在无需人工深度标注的前提下，实现鲁棒的动态 4D 场景重建，并能提供不确定性估计。

## 2. 方法论
- 核心思想：将 **Riemannian Flow Matching（RFM）** 引入 4D 高斯泼溅的参数优化中，在非欧几里得流形上定义概率路径：
  - 求解对象包括高斯的尺度（scale）、旋转（rotation）和不透明度（opacity）等参数。
  - 这些参数本身具有流形结构，直接在欧氏空间插值/优化会走到非法区域；在流形上定义路径可保证所有中间状态数学有效。
- 技术细节：
  - 使用概率路径连接数据分布与先验分布，通过流匹配学习速度场。
  - 结合测试时优化（Test-Time Optimization, TTO）进一步适配具体场景。
  - 通过受控实验将 RFM 的贡献与 TTO、预训练分离。
- 由于正文不可得，论文未提供更具体的公式或算法伪代码。

## 3. 实验设计
- 摘要中提到的实验包括：
  - 受控对比实验：使用相同数据、架构和 TTO，比较确定性 MLP 基线与 RFM 的性能。
    - MLP 基线：F-score 0.762。
    - RFM：F-score 0.806。
    - 结论：+0.044 的增益被归结为 RFM 的独立贡献。
  - 与先前模型的对比：DAV4 在动态重建和新视角合成上优于之前的 Depth Anything 模型和逐场景 4D-GS 方法。
- 未提及具体使用哪些公开数据集或基准（benchmark）名称；可能涉及动态场景重建的常见评测集，但现有信息不足以确认。
- 对比方法的设置、度量指标（除 F-score 外）等细节在摘要中未展开。

## 4. 资源与算力
- 摘要明确给出：预训练需要 **360 GPU 小时**。
- 未说明 GPU 型号、数量、单卡还是多卡、推理成本等。
- 作者指出该成本可在超过 10,000 个场景的大规模部署中被摊销。

## 5. 实验数量与充分性
- 现有摘要仅明确提到了 **一个受控消融实验**（MLP vs RFM）和一组与 prior 方法的对比。
- 由于完整论文被撤回，无法获知是否有更多数据集上的实验、多组消融、不确定性评估细节（如负高斯对数似然、期望校准误差等）。
- 从信息量看，实验覆盖有限：
  - 缺乏跨数据集泛化验证；
  - 缺乏与更多最新 4D 重建方法的对比；
  - 缺乏对 TTO 与预训练组合效果的全面分析。
- 因此，无法判断实验是否充分、客观、公平；只能确认在所述单一对比中 RFM 有明确增益。

## 6. 主要结论与发现
- RFM 对动态 4D 重建有独立且可量化的贡献：在不同方法、相同条件下带来 F-score 提升 +0.044（0.762→0.806）。
- 通过流形概率路径设计，可避免欧氏空间优化导致的无效高斯参数，从而提升重建质量。
- DAV4 在无人工深度标签条件下，动态重建和新视角合成能力优于先前 Depth Anything 系列和逐场景 4D-GS。
- 模型能够输出不确定性估计（负高斯对数似然、期望校准误差），有助于可靠性分析。
- 预训练成本虽然较高，但在大规模场景部署时具有经济可行性。

## 7. 优点
- **理论合理性**：针对高斯参数的非欧氏几何，使用 Riemannian Flow Matching 而非普通欧氏插值/回归，数学上更严谨。
- **不依赖人工深度标签**：减轻了数据标注负担，有利于开放场景应用。
- **可分离分析**：通过受控实验将 RFM 的贡献从 TTO 和预训练中剥离，体现了较好的实验设计意识。
- **不确定性量化**：提供 NLL 和 ECE 等指标，增强了模型输出的可信度评估。
- **扩展性视角**：给出了算力摊销分析，适合大规模部署场景。

## 8. 不足与局限
- **论文撤回/不可用**：当前版本已被撤回，无法验证方法完整细节、实验结果和复现性。
- **实验透明度不足**：未明确数据集、评价基准、具体对比方法配置，尚未提供可复现代码或详细设置。
- **算力门槛较高**：360 GPU 小时预训练成本不低，对于小规模研究组或少量场景应用可能不划算；摊销优势仅在超万级场景下才明显。
- **单点增益是否稳健存疑**：+0.044 的 F-score 提升仅来自单一对比，缺乏跨数据集、跨场景的重复验证。
- **潜在偏差风险**：如果 RFM 的实现、流形度量选择或训练配置存在倾向性，可能影响结论的客观性；在无完整论文的情况下无法排除。
- **应用限制**：动态 4D 重建通常对内存和实时性敏感，摘要未讨论推理效率、存储开销以及长视频中的漂移问题。

（完）
