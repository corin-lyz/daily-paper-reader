---
title: Few-Shot Open-Vocabulary Remote Sensing Segmentation via Textual Inversion
title_zh: 基于文本反转的少样本开放词汇遥感图像分割
authors: "Junhyuk Heo, Junghwan Park"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25563v1"
tags: ["query:seg"]
score: 8.0
evidence: 通过文本反转的开放词汇分割
tldr: 开放词汇分割在遥感图像上表现不佳，主要原因是类别名称作为文本查询在视觉-语言嵌入空间中地址较弱。通过冻结分割模型，对少量样本进行文本反转学习更优的文本嵌入，无需更改模型或视觉提示。在代表性基准上，受影响类别的平均交并比从3.9提升至39.4，并在八个遥感数据集上优于注入视觉提示的少样本方法。该方法有效弥补了遥感场景下文本查询的不足，仅需少量样本即可显著提升性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25563v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 996, \"height\": 618, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1269, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1276, \"height\": 438, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1256, \"height\": 925, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 585, \"height\": 587, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25563v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 561, \"height\": 459, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25563v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1271, \"height\": 553, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25563v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 353, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25563v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1183, \"height\": 441, \"label\": \"Table\"}]"
motivation: 开放词汇分割在遥感图像上性能差，原因是类别名作为文本查询在嵌入空间中地址弱，自然语言改写无法完全修复。
method: 冻结分割模型，对少量支持样本进行文本反转，学习更优的文本嵌入，推理时仅使用文本。
result: 受影响类别mIoU从3.9提升至39.4，在八个遥感数据集上优于注入视觉提示的少样本方法。
conclusion: 少样本文本反转有效提升遥感开放词汇分割，无需更改模型或视觉信息，仅需少量样本即可弥补文本查询弱点。
---

## 摘要
开放词汇分割可以从文本查询中标记任意类别，无需针对每个类别进行训练，但在遥感图像上，其对于在其他场景中处理良好的类别表现不佳。我们发现，这一差距很大程度上源于文本查询而非分割模型本身。由于这些模型并非专门针对俯拍图像设计，作为查询的类别名称通常难以在视觉-语言嵌入空间中准确定位。我们证明，更好的名称可以部分弥补这一差距，而剩余的失败则需要一种经过测试的自然语言改写所无法提供的定位方式。我们通过冻结模型上的文本反转，从少量样本中恢复该定位，并仅使用推理文本。在一个代表性基准测试中，受影响的类别平均交并比从3.9提升至39.4，并且在八个遥感数据集上，该方法优于那些在推理时注入视觉提示的少样本方法。

## Abstract
Open-vocabulary segmentation labels arbitrary categories from a text query without per-class training, yet on remote sensing imagery it underperforms on categories it handles reliably elsewhere. We find that much of this gap traces to the text query rather than to the segmentation model. Because these models are not specialized for overhead imagery, the class name that serves as the query is often a weak address into the vision-language embedding space. We show that a better name repairs part of the gap, while the remaining failures call for an address that the tested natural-language rephrasings do not provide. We recover that address from a few examples through textual inversion on a frozen model, keeping inference text only. On a representative benchmark this raises the mean intersection over union on the affected categories from 3.9 to 39.4, and across eight remote sensing datasets it improves over few-shot methods that instead inject visual prompts at inference.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：开放词汇分割模型能够根据文本查询分割任意类别，无需针对每个类别进行训练。然而，在遥感图像（俯拍影像）上，这些模型对于在其他场景（如自然图像）中表现良好的类别，性能却显著下降。
- **根源**：作者发现，性能差距主要来自文本查询而非分割模型本身。由于视觉-语言模型（如CLIP）并非专门针对俯拍图像设计，类别名称作为文本查询时，在视觉-语言嵌入空间中往往“地址较弱”，难以准确定位到遥感图像对应的视觉特征。
- **背景**：现有尝试（如自然语言改写类别名称）只能部分弥补差距，但无法完全解决剩余的失败情况，说明需要一种更精细的、基于视觉示例的地址修正方式。因此，研究动机是探索一种无需修改模型结构、仅通过少量样本改进文本查询的方法，以提升遥感开放词汇分割性能。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：利用**文本反转（Textual Inversion）**技术，从少量支持样本（few-shot）中学习更优的文本嵌入向量，用来替代原始的类别名称作为查询。该方法完全冻结分割模型（包括视觉编码器和文本编码器），仅优化文本嵌入（类似于一个可学习的“伪词”），推理时仅使用文本，无需任何视觉提示。
- **关键技术细节**：
  - **冻结模型**：预训练的开放词汇分割模型（如基于CLIP的分割模型）的所有参数保持不变。
  - **支持样本**：对每个目标类别，提供少量（如1-5张）带有分割标注的遥感图像作为支持集。
  - **文本反转**：定义一个可学习的嵌入向量（placeholder token），通过最小化支持集图像特征与文本嵌入之间的对比损失（或分割损失），优化该嵌入向量，使其在嵌入空间中与支持样本的视觉特征对齐。
  - **推理阶段**：仅使用学到的文本嵌入进行分割，无需支持样本或视觉提示。
- **公式/算法流程（文字说明）**：
  1. 输入：预训练的分割模型 \(M\)，目标类别 \(c\) 的少量支持图像-掩码对（\(I_i, M_i\)）。
  2. 初始化：为类别 \(c\) 创建一个可学习的嵌入向量 \(v_c\)（维度与文本编码器输出一致）。
  3. 优化：冻结模型 \(M\)，对每个支持图像，提取图像特征；同时将 \(v_c\) 输入文本编码器得到文本特征。计算分割损失（如像素级的对比损失或Dice损失），反向传播更新 \(v_c\)。
  4. 测试：对任意测试图像，将学到的 \(v_c\) 作为文本查询，通过模型预测类别 \(c\) 的分割掩码。

## 3. 实验设计

- **数据集与场景**：
  - 一个代表性基准测试（具体名称在元数据中未给出，但可能为常见遥感分割基准如LoveDA、Potsdam等），用于评估受影响类别的性能提升。
  - 额外在**八个遥感数据集**上进行广泛评估，覆盖不同场景（如城市、农业、水域等）。
- **Benchmark**：以平均交并比（mIoU）为主要指标，重点关注“受影响类别”（即在自然图像上表现好但在遥感上差的类别）。
- **对比方法**：
  - 原始开放词汇分割模型（使用原始类别名称）。
  - 使用经过自然语言改写的类别名称。
  - **注入视觉提示的少样本方法**（例如通过添加支持图像的特征或使用视觉提示调整）。作者的方法仅使用文本，与这类需要视觉提示的方法进行对比。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提及方法基于冻结模型，因此训练成本较低（仅优化少量嵌入参数），但具体硬件资源未披露。

## 5. 实验数量与充分性

- **实验数量**：
  - 核心实验：在一个代表性基准测试上，受影响类别的mIoU从3.9提升至39.4（约10倍提升）。
  - 跨数据集验证：在8个遥感数据集上进行了比较，证明方法优于注入视觉提示的少样本方法。
  - 可能包含消融实验（如不同支持样本数量、是否使用文本反转等），但摘要中未详细列出。
- **充分性与客观性**：
  - 实验覆盖范围较广（8个数据集），且对比了多种基线（原始查询、改写查询、视觉提示方法），比较公平。
  - 但是，未提供与全监督方法的对比，也未分析在未见类别上的泛化能力。实验规模尚可，但缺乏对模型复杂度、推理速度的讨论。总体来说，实验设计能够支撑核心论点，但仍有细节待补充。

## 6. 主要结论与发现

- 开放词汇分割在遥感图像上的性能缺陷主要源于**文本查询的弱地址性**，而非模型本身的不足。
- 通过**文本反转**从少量样本中学习更优的文本嵌入，可以显著弥补这一差距，使受影响类别的mIoU从3.9提升到39.4。
- 该方法在8个遥感数据集上超越了注入视觉提示的少样本方法，表明纯文本的、仅需少量样本的修复方式更有效且更简洁。
- 仅需少量样本（few-shot）即可获得大幅提升，无需更改模型结构或依赖视觉提示。

## 7. 优点（方法或实验设计亮点）

- **简洁高效**：仅通过优化文本嵌入实现性能飞跃，无需微调整个模型或引入复杂模块。
- **推理时纯文本**：测试阶段不需要支持样本或额外特征，保持了开放词汇分割的灵活性和推理效率。
- **通用性强**：适用于任何基于视觉-语言模型的分割框架，可轻松嵌入现有系统。
- **实验覆盖全面**：在多个遥感数据集上验证，且与多种少样本方法对比，证明了方法在遥感场景中的有效性。

## 8. 不足与局限

- **依赖少量标注**：需要每个目标类别提供少量带掩码的支持样本，这在实际应用中可能仍有标注成本。
- **未在新类别上测试**：方法假设目标类别在支持集中出现，对于完全未见过的类别无法应用（除非使用通用文本嵌入）。论文未评估类别泛化能力。
- **算力信息缺失**：未报告训练所需的具体GPU资源，不利于复现和成本评估。
- **实验细节有限**：摘要中未给出数据集名称、支持样本数量、消融实验的具体设置，以及与其他强基线（如全微调方法）的对比，因此实验的充分性还有待全文验证。
- **潜在偏差**：仅针对遥感俯拍图像，在自然图像或其他领域（如医疗影像）的效果不明；且“受影响类别”的定义可能依赖特定模型，存在模型选择偏差。

（完）
