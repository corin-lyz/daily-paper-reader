---
title: A Unified Stereo Geometry Estimation Framework for Disparity and Surface Normal
title_zh: 一种统一的视差与表面法向估计的立体几何框架
authors: "Qizhe Wei, Xianda Guo, Shaocong Xu, Hong Li, Runyi Yang, Hao Zhao"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24024v1"
tags: ["query:stereo-depth"]
score: 8.0
evidence: 统一的立体几何估计框架，联合预测视差和表面法线
tldr: 现有前馈立体匹配方法在低光、高反射等挑战区域缺乏强几何先验，预测不可靠。本文提出GeoStereo统一框架，联合预测视差和表面法线，通过耦合前馈立体匹配与扩散模型法线分支，并引入视差到法线初始化及左视图条件实现有效交互。零样本下，在KITTI、NYUv2等基准上视差估计达到Rank-1，在iBims-1、ScanNet上法线估计最优。该工作为立体几何估计提供强结构先验，在困难场景表现出色。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24024v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1800, \"height\": 740}, {\"url\": \"assets/figures/arxiv/2607.24024v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 884, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2607.24024v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1809, \"height\": 786}, {\"url\": \"assets/figures/arxiv/2607.24024v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1840, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2607.24024v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1840, \"height\": 1085}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24024v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1795, \"height\": 323}, {\"url\": \"assets/tables/arxiv/2607.24024v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1805, \"height\": 582}, {\"url\": \"assets/tables/arxiv/2607.24024v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1666, \"height\": 316}, {\"url\": \"assets/tables/arxiv/2607.24024v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 534}, {\"url\": \"assets/tables/arxiv/2607.24024v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 876, \"height\": 197}]"
motivation: 现有立体匹配在挑战区域因缺乏几何先验而预测不可靠，需引入强结构先验提升鲁棒性。
method: 提出GeoStereo，耦合前馈立体匹配与扩散法线分支，通过视差-法线初始化和左视图条件实现双向交互。
result: 零样本下，在KITTI、NYUv2等基准视差估计Rank-1，在iBims-1、ScanNet法线估计最优。
conclusion: GeoStereo统一框架利用扩散先验显著提升立体几何估计在困难场景的准确性和鲁棒性。
---

## 摘要
立体匹配和表面法向估计是三维视觉中的基本任务。然而，现有的前馈式立体方法在挑战性区域中仍难以产生可靠预测，主要原因是缺乏强几何先验。本文提出GeoStereo，一种统一的立体几何估计框架，利用强大的扩散先验联合预测视差和表面法向。具体来说，GeoStereo将前馈式立体匹配流水线与基于扩散的法向估计分支相结合。为了促进两个任务之间的有效交互，我们引入了一种从视差到法向的初始化策略，并为扩散过程构建了扭曲到左视条件。这种耦合设计使得扩散分支能够提供强大的结构先验，从而增强不适定区域的视差估计，同时前馈分支为准确的法向预测提供可靠的几何指导。大量实验表明，GeoStereo在挑战性场景（包括低光照环境、高反射表面和透明物体）中表现可靠。在零样本设置下，它在多个基准（包括KITTI和NYUv2）上实现了排名第一的视差估计，并在许多真实室内基准（如iBims-1和ScanNet）上提供了最佳的法向估计精度。项目页面：https://qz-wei.github.io/GeoStereo.github.io/

## Abstract
Stereo matching and surface normal estimation are fundamental tasks in 3D vision. However, existing feed-forward stereo methods still struggle to produce reliable predictions in challenging regions, mainly due to the lack of strong geometric priors. In this paper, we propose $\textbf{GeoStereo}$, a unified stereo geometry estimation framework that leverages powerful diffusion priors to jointly predict disparity and surface normals. Specifically, GeoStereo couples a feed-forward stereo matching pipeline with a diffusion-based normal estimation branch. To enable effective interaction between the two tasks, we introduce a disparity to normal initialization strategy and construct a warp to left-view condition for the diffusion process. This coupled design allows the diffusion branch to provide strong structural priors that enhance disparity estimation in ill-posed regions, while the feed-forward branch offers reliable geometric guidance for accurate normal prediction. Extensive experiments show that GeoStereo performs reliably in challenging scenarios, including low-light environments, highly reflective surfaces, and transparent objects. Under zero-shot settings, it achieves Rank-1 disparity estimation on multiple benchmarks, including KITTI and NYUv2, and delivers the best normal estimation accuracy on many real indoor benchmarks, such as iBims-1 and ScanNet. Project page: https://qz-wei.github.io/GeoStereo.github.io/

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：立体匹配和表面法线估计是三维视觉的基本任务。现有前馈式立体匹配方法（如FoundationStereo）在常规场景精度高，但在极低光照、高反射表面、透明物体等挑战性区域预测不可靠。作者归因于训练数据的长尾分布和前馈范式的数据饥饿特性。
- **总体目标**：提出GeoStereo，一种统一框架，同时预测视差（disparity）和表面法线（surface normal），利用扩散模型强大的先验知识来增强立体匹配在困难区域的鲁棒性，同时利用立体几何为法线估计提供可靠指导。

## 2. 论文提出的方法论

### 核心思想
- 将前馈立体匹配网络与基于扩散的法线估计分支耦合在一个多任务框架中。
- 通过**视差到法线初始化**（Disparity to Normal Initialization）和**扭曲到左视图条件**（Warp to Left-View Condition）实现两个任务的有效交互。
- 扩散分支为立体匹配提供结构先验，立体分支为法线估计提供几何指导。

### 关键技术细节

#### 立体匹配分支
- 使用DepthAnythingV2初始化特征提取器，构建混合代价体（hybrid cost volume），采用FoundationStereo的架构。
- 通过Soft Argmin得到初始视差 \(D_{init}\)，再通过ConvGRU更新模块得到最终视差 \(D_{final}\)。
- 损失函数为 \(L_{disp} = \lambda_{init} \|D_{init} - D_{gt}\|_1 + \|D_{final} - D_{gt}\|_1\)。

#### 视差到法线初始化
- 将初始视差转换为伪深度 \(Z(u,v) = -\log(\text{clip}(D_{init}/d_{max}, \epsilon, 1))\)。
- 构建3D点图 \(P(u,v) = [x(u)Z(u,v), y(v)Z(u,v), Z(u,v)]^T\)。
- 通过中心差分计算局部切向方向，叉积得到法线并归一化，得到几何先验 \(N_{init}\)。

#### 扭曲到左视图条件
- 利用初始视差通过可微分的后向扭曲将右视图扭曲至左视图坐标：\(I_{r\to l}(u,v) = I_r(u - D_{init}(u,v), v)\)，提供空间对齐的跨视图外观条件。

#### 扩散法线估计分支
- 基于Stable Diffusion v2.1，采用\(x_0\)重参数化（预测干净潜变量）。
- 条件包括：左视图RGB、扭曲后的右视图、以及法线初始化 \(N_{init}\)。
- 损失函数 \(L_{norm} = \mathbb{E}_{x_0, t, \epsilon} \| x_0 - \mu_\zeta(x_t, t, N_{init}, f_\chi(I_{r\to l}))\|_2^2\)。

#### 一致性训练策略
- 两阶段训练：
  - 第一阶段：分别优化立体分支和法线分支。
  - 第二阶段：联合优化，总损失 \(L = L_{disp} + \lambda_{norm} L_{norm}\)。

### 算法流程（文字描述）
1. 输入立体图像对 \((I_l, I_r)\)。
2. 特征提取和代价体构建，得到初始视差 \(D_{init}\)，然后Refine得到 \(D_{final}\)。
3. 从 \(D_{init}\) 计算几何法线先验 \(N_{init}\)，并将 \(I_r\) 扭曲到左视图得 \(I_{r\to l}\)。
4. 将 \(I_l, I_{r\to l}, N_{init}\) 作为条件输入扩散U-Net，经过迭代去噪、VAE解码得到最终法线图 \(N_{final}\)。
5. 梯度从法线损失反向传播至立体分支，实现双向交互。

## 3. 实验设计

### 使用的数据集
- **训练数据**：
  - 立体匹配分支：混合数据集约150万对视差-图像对，包括FSD、SceneFlow、Sintel、CREStereo、FallingThings、StereoCarlaV2、IRS、3D Ken Burns、Structured3D、InteriorVerse、DIODE、Virtual KITTI 2以及自采集数据。
  - 法线分支：约60万对图像-法线对，来自HyperSim、3D Ken Burns、Structured3D、IRS、InteriorVerse、MatrixCity（小规模）和新渲染的包含200个场景6万张图像的数据集（Blender渲染）。
- **测试 benchmark**（零样本）：
  - **视差**：Hypersim、DREDS、KITTI、ScanNet、NYUv2。
  - **法线**：iBims-1、NYUv2、DIODE、ScanNet。

### 对比方法
- **立体匹配**：LightStereo、Monster、S2M2、FoundationStereo。
- **法线估计**：DSINE、GeoWizard、StableNormal、Marigold。

### 评价指标
- 视差：EPE（端点误差）、D1 outlier率。
- 法线：平均角误差、中值角误差、<11.25° / <22.5° / <30° 像素百分比。

## 4. 资源与算力

- 文中提到：
  - 立体模块：使用AdamW优化器，初始学习率2×10⁻⁵，在4块NVIDIA H20 GPU上以总批大小16训练。
  - 法线模块：基于Stable Diffusion v2.1，使用AdamW优化器，固定学习率1×10⁻⁵，在4块NVIDIA H20 GPU上以总批大小32训练。
- **未明确说明**：训练总时长（epoch/迭代次数）、两阶段训练各阶段的具体步数。此外，也未见推理时间或模型参数量等计算成本信息。

## 5. 实验数量与充分性

- **零样本对比实验**：在5个视差基准和4个法线基准上进行了定量对比，全面覆盖室内外、合成/真实场景。
- **消融实验**：
  - 消融不同法线估计设计（顺序/并行/扩散 vs 前馈）在IRS数据集上。
  - 消融扭曲到左视图条件（比较左视图、右视图、扭曲左视图）在3D Ken Burns和IRS上。
  - 消融视差到法线初始化（w/ vs w/o D2N）在IRS上。
- **定性对比**：展示多种困难场景（低光、反射、透明物体）的视觉比较。
- **充分性评价**：实验设计较为充分，覆盖了多种数据集、baseline和关键组件消融；但消融实验仅在少数数据集（主要是IRS和3D Ken Burns）上进行，缺少更广泛基准的消融结果。此外，与更多最新方法（如Stereo Anywhere等）的比较未包含；法线对比方法虽然代表性强，但Monocular Depth加表面法线的联合方法（如Metric3D v2）未参与法线对比。

## 6. 论文的主要结论与发现

- GeoStereo在零样本设置下取得有竞争力的性能：视差估计在KITTI、NYUv2等上达到Rank-1，法线估计在iBims-1、ScanNet上达到最优或接近最优。
- 在挑战性区域（弱纹理、反射、透明、低光）显著优于纯前馈立体匹配方法。
- 扩散分支提供的结构先验和立体几何提供的条件有效改善了法线预测的质量（更锐利的边界、更完整的结构）。
- 所有设计的组件（并行扩散设计、扭曲左视图条件、视差到法线初始化）都带来性能提升。

## 7. 优点

- **创新性**：首次在立体匹配中联合扩散模型进行法线估计，实现双向交互，而不是简单的后处理或特征共享。
- **技术设计巧妙**：视差-法线初始化为几何先验，扭曲左视图提供跨视图对齐条件，且两个模块均可微分，使梯度双向传播。
- **零样本泛化强大**：在多个不同领域的基准上无需微调即达到SOTA或竞争力强的结果。
- **挑战场景鲁棒性**：定性结果清晰展示了在反射、透明、低光区域的改进。
- **全面的消融实验**：验证了各个设计组件的贡献。

## 8. 不足与局限

- **计算效率低**：由于引入扩散模型作为法线分支，增加了额外计算开销，作者在结论中也承认效率有待提升。
- **消融实验覆盖不够广**：消融仅针对IRS和3D Ken Burns两个数据集，缺少在更多基准（如KITTI、NYUv2）上的消融验证。
- **与其他基线对比不完全**：未能与最新联合深度/法线估计方法（如Metric3D v2）进行法线对比，也未与更多立体匹配最新论文（如Stereo Anywhere）对比。
- **训练细节部分缺失**：未提供训练迭代次数、学习率衰减细节、两阶段训练各阶段具体步数、推理时间、模型参数量等，可复现性略减。
- **未涉及室外法线基准**：法线评估全部为室内场景，缺少对室外场景（如KITTI的法线评估）的验证，泛化性验证尚不完整。
- **数据集标注偏差**：训练数据主要为合成数据和少数真实数据，真实世界场景分布可能存在偏差，部分挑战场景（如户外夜间）未充分验证。

（完）
